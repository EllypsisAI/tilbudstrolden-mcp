# 2026-05-21

## [pre-planning] DB + hosting + repo locks, plugin tooling enabled, subagent permissions allowlisted

Cross-session prep before user opens a fresh session to do the actual station-1 planning. Nothing was spec'ed or committed as decision-record this session — those happen in the next one. What got settled:

**Locked decisions (need formal records during next session's station-1 work):**

- **DB: Postgres-everywhere.** Docker-compose locally, **Neon serverless Postgres** in prod. Identical wire protocol both sides, mature migrations (Drizzle/Prisma), branching for cheap per-env staging, scale-to-zero pricing matches usage-based revenue. Lock-in risk is bounded — `pg_dump | pg_restore` to RDS/Supabase if Neon goes sideways, mechanical not rewrite. User leaned SQLite-in-Docker initially; rejected after walking through scaling cliff (single-writer multi-tenant), backup story (litestream homework), and migration tooling gaps. "Needs to be correct from the start" was incompatible with SQLite's plan-to-port-later trajectory.
- **Hosting: central-hosted multi-tenant.** Settles alignment open question §1 (multi-user hosting model). Users install an MCP connector pointing at our URL; they run nothing locally except `docker-compose up` for our own dev. Auto-settles §2 (scheduled tasks) — they live on our server, normal cron.
- **Scheduling-as-moat (good surprise, capture as part of work):** I initially misread user's "scheduling" as server-side scheduling. User meant client-side (ChatGPT/Claude getting a robust "schedule this prompt" feature). The misread surfaced a real moat angle worth keeping: server pre-computes weekly deals digests, daily pantry-expiry checks, monthly billing tallies — so when client-side scheduling matures and triggers our MCP at 6am Sunday, the answer is already cooked. Doubly valuable. Don't nuke; build toward.
- **Repo strategy: stay public on EllypsisAI/tilbudstrolden-mcp fork.** Verified facts: it's a public fork of olgasafonova/tilbudstrolden-mcp, MIT license (full freedom — modify/sell/sublicense, only obligation is keeping copyright notice in derived files). No formal partnership with Olga. User chose not to detach-and-privatize (workaround was documented: ~5 min, `git push --mirror` to new private repo, breaks fork link). Soft hard-fork mindset: stop tracking upstream, keep Olga's MIT copyright header in `src/api.ts` / `src/store.ts` / original tool implementations, optional courtesy DM to Olga ("your code inspired this, going hard agent-native"). No legal need, just nice.

**Plugin tooling now active:**

- `mcp-server-dev@claude-plugins-official` enabled. Skills loaded: `build-mcp-server` (entry point for deployment-model decisions), `build-mcp-app` (UI-tool registration), `build-mcpb` (local-bundle packaging).
- `mcp-apps@mcp-apps` reinstalled (had a marketplace SSH auth glitch — switching to HTTPS install path resolved it). Skills loaded: `create-mcp-app`, `add-app-to-server`, `convert-web-app`, `migrate-oai-app`. Phase-1 digest at `workspace/raw/mcp-apps-docs-digest.md` remains the canonical narrative summary; skills are now invokable for procedural guidance.
- Plugins to hold for later: `frontend-design` (station 4), `skill-creator` (station 6), `mcp-eval@ellypsis-labs` (station 5 or 8).

**Subagent permission allowlist landed in `.claude/settings.json`:**

User's directive: "agents aren't gonna route through u but work as ur team — they need to move freely in the repo and use the same tools as u." Project-level rules now allow Read/Glob/Grep/Edit/Write/NotebookEdit, Agent/ToolSearch, Task*, EnterPlanMode/ExitPlanMode, WebSearch/WebFetch, Bash (with destructive deny), all EXA tools, all Firecrawl scrape/search/extract/map/crawl tools, Context7. Ask-list: `git push`, `git commit`, `gh pr create`/`close`/`merge`, `gh issue create`/`close`, `gh release`, `npm install`/`uninstall`/`publish`. Deny-list: `rm -rf:*`, `git push --force/-f`, `git reset --hard`, `git clean -fd`, `git branch -D`, `git checkout --`. **Why it matters:** earlier this session a background research subagent was blocked from every web tool because there was no allowlist; main agent worked via interactive prompts, subagents had no prompt surface. Now fixed at project scope (committed to repo).

**Operating patterns saved to persistent memory:**

`~/.claude/projects/.../memory/feedback_operating_patterns.md` — orchestrator-with-subagents model, briefing template (inline context + acceptance criteria + don'ts + return summary + verify), Sonnet-for-routine/Opus-for-judgment, verification per chunk not at end, decisions-with-rationale, ask-before-commit, scope discipline, terse comms. Survives across sessions. Conveyed by user pasting their other-project's pattern summary.

**Tasks created but not executed:**

#1 inspect code baseline (in_progress, no work done — aborted before reading), #2 external research (in_progress, blocked by permission issue now fixed), #3 on-disk mcp-apps examples (in_progress, not started), #4 synthesize, #5 present plan for approval. **Next session should re-claim these or rewrite; the research-blocker that caused the abort is no longer relevant.**

**Open / next session:**

- **User is starting fresh.** Read `workspace/alignment.md`, `workspace/roadmap.md`, all `workspace/decisions/`, the most recent 3-5 journals (this one + 2026-05-19 + 2026-05-18). Then re-pick up at station 1.
- **Station 1 = produce three artifacts:** `workspace/specs/01-hosting-and-db.md` (co-written with user), `workspace/decisions/hosting-model.md` (central-hosted multi-tenant, rationale + tradeoffs grounded), `workspace/decisions/db-choice.md` (Neon Postgres + docker-compose locally, rationale + tradeoffs grounded). Plus any `alignment.md` shifts (probably none — decisions are downstream of locked architecture), plus roadmap status update.
- **Grounding requirement:** user explicitly said plan based on real merit from sources, not training data. Use the now-permissioned WebSearch/WebFetch/EXA tools for external citations (Neon docs, MCP deployment patterns, multi-tenant patterns). Use the loaded `build-mcp-server` / `build-mcp-app` skills for procedural guidance. Use `Plan` subagent for second-opinion architectural review before exiting plan mode.
- **Mode discipline:** docs aren't code, so technically `ExitPlanMode` isn't required, but user explicitly asked to route through plan mode/plan agent. Workflow per user's other-project patterns: research → draft → second-opinion subagent → enter plan mode → write plan → ExitPlanMode for approval → execute writes.
- **Repo-strategy item that doesn't bind station 1:** if/when station 2 (JSON→DB migration) starts touching substantive code, drop Olga the courtesy DM beforehand. Out of scope for station 1.
- **Sidetrack to remember at station 6:** `claude/agent-native-food-app-9g04a` (parallel `start_onboarding` implementation on the old skill-distribution understanding). Evaluate then; ignore until then.
- **Branch discipline:** stay on `claude/agent-native-food-app-Mb6B3`. Don't push main. Don't open PRs without explicit ask.
